// Optional. Leave the key blank to use the built in offline starter steps,
// which means the app works with zero setup. If you want real AI generated
// micro tasks, paste an Anthropic API key here.
//
// Note: a key placed here ships inside your personal build. That is acceptable
// for an app only you install. Never do this for an app you distribute.

export const ANTHROPIC_API_KEY = "";
